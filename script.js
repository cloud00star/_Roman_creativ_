document.addEventListener('DOMContentLoaded', () => {
    alert("Добро пожаловать на мой сайт!");
});
import { server } from "./script";

// run with `node server.mjs`
npxhttp - server(http); //localhost:8000)